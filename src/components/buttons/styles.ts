import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  buttonDelete: {
    color: "#808080",
  },
  buttonCreate: {
    backgroundColor: "#1E6F9F",
    color: "#FFF",
    padding: 18,
    borderRadius: 6,
  },
});
